#!/usr/bin/env bash

function main {
  local shome="${_docker_machine_home:-"$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"}"
  source "$shome/script/profile"

  if [[ -x "$(which docker-machine 2>/dev/null || true)" ]]; then
    return 1
  fi

  return 0
}

source sub "$0" "$@"
